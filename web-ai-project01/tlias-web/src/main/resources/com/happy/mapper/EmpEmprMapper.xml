<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.happy.mapper.EmpEmprMapper">
    <insert id="insert">
        insert into emp_expr(emp_id,begin,end,company,job) values
        <foreach collection="empExprList" item="expr" separator=",">
            (#{expr.empId},#{expr.begin},#{expr.end},#{expr.company},#{expr.job})
        </foreach>
    </insert>
    <update id="updateEmpr">
        update emp_expr set emp_id=#{empid}
        <foreach collection="empExprList" item="expr" separator=",">
            company=#{expr.company},job=#{expr.job},begin=#{expr.begin},end=#{expr.end}
        </foreach>
        where emp_id=#{empid}
    </update>
    <delete id="deleteEmpr">
        delete from emp_expr where emp_id in
        <foreach collection="ids" item="empid" separator="," open="(" close=")">
            #{empid}
        </foreach>
    </delete>
    <select id="findBackEmpr" resultType="com.happy.pojo.EmpExpr">
        select * from emp_expr where emp_id=#{id}
    </select>
</mapper>