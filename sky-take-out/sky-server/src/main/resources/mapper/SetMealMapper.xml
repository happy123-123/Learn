<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetMealMapper">
    <insert id="addSetMeal" useGeneratedKeys="true" keyProperty="id">
        insert into sky_take_out.setmeal (name, category_id, price, status, description, image, create_time, update_time, create_user, update_user)
        values (#{name}, #{categoryId}, #{price}, #{status}, #{description}, #{image} ,#{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>
    <update id="updateSetMeal">
        update sky_take_out.setmeal
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="image != null">
                image = #{image},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser},
            </if>
        </set>
    </update>
    <update id="control">
        update sky_take_out.setmeal left join sky_take_out.setmeal_dish on sky_take_out.setmeal.id = sky_take_out.setmeal_dish.setmeal_id
        <set>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser},
            </if>
        </set>
        where dish_id = #{id}
    </update>
    <update id="controlSetMeal">
        update sky_take_out.setmeal
        <set>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser},
            </if>
        </set>
        where id = #{id}
    </update>
    <delete id="deleteDish">
        delete from sky_take_out.setmeal where id in
        <foreach collection="ids" item="id" separator="," close=")" open="(">
            #{id}
        </foreach>
    </delete>

    <select id="count" resultType="java.lang.Long">
        select count(*) from sky_take_out.setmeal
    </select>
    <select id="findpageQuery" resultType="com.sky.entity.Setmeal">
        select * from sky_take_out.setmeal
        <where>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="name != null">
                and name like concat(#{name},'%')
            </if>
        </where>
        limit #{page},#{pageSize}
    </select>
    <select id="findById" resultType="com.sky.entity.Setmeal">
        select * from sky_take_out.setmeal where id = #{id}
    </select>
    <select id="findByCtg" resultType="com.sky.entity.Setmeal">
        select * from sky_take_out.setmeal
        <where>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>
    <select id="getOverViewSetmeal" resultType="java.lang.Integer">
        select count(*) from sky_take_out.setmeal where status = #{status}
    </select>
</mapper>