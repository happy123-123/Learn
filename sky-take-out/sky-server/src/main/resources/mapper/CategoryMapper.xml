<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.CategoryMapper">
    <insert id="addAll">
        insert into sky_take_out.category (type, name, sort, create_time, update_time, create_user, update_user)
        values (#{type}, #{name}, #{sort}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>
    <update id="updateAll">
        update sky_take_out.category
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="type != null">
                type = #{type},
            </if>
            <if test="sort != null">
                sort = #{sort},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser},
            </if>
        </set>
        where id = #{id}
    </update>
    <update id="controlAll">
        update sky_take_out.category
        set status = #{status}
        where id = #{id}
    </update>
    <delete id="deleteAll">
        delete from sky_take_out.category
        where id = #{id}
    </delete>
    <select id="count" resultType="java.lang.Long">
        select count(*) from sky_take_out.category
    </select>
    <select id="findByPage" resultType="com.sky.entity.Category">
        select * from sky_take_out.category
        <where>
            <if test="name != null and name != ''">
                and name like concat(#{name},'%')
            </if>
            <if test="type != null">
                and type = #{type}
            </if>
        </where>
    </select>
    <select id="findCtgByType" resultType="com.sky.entity.Category">
        select * from sky_take_out.category
        where status = 1
        <if test="type != null">
            and type = #{type}
        </if>
        order by #{sort}
    </select>
</mapper>